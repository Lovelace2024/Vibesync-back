/*import {
    RedisClientType,
    RedisFunctions,
    RedisModules,
    RedisScripts,
    createClient
} from 'redis';

export const redisClient: RedisClientType<RedisModules, RedisFunctions, RedisScripts> = await createClient()
    .on('error', err => console.log('Redis Client Error', err))
    .connect();*/
